import "./{{kebabCase name}}.element.js";

import type { Meta, StoryObj } from "@storybook/web-components";
import { html } from "lit";

import type { USA{{pascalCase name}}Element } from "./{{kebabCase name}}.element.js";

// More on how to set up stories at: https://storybook.js.org/docs/writing-stories
const meta = {
  title: "{{kebabCase name}}",
  tags: ["autodocs"],
  render(args) {
    return html`<usa-{{kebabCase name}}></usa-{{kebabCase name}}>`;
  },
  argTypes: { },
  args: { },
} satisfies Meta<USA{{pascalCase name}}Element>;

export default meta;

type Story = StoryObj<USA{{pascalCase name}}Element>;

// More on writing stories with args: https://storybook.js.org/docs/writing-stories/args
export const Primary: Story = {
  args: {},
};
