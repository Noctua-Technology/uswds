import "./{{kebabCase name}}.element.js";

import { fixture, html, assert } from "@open-wc/testing";

import { USA{{pascalCase name}}Element } from "./{{kebabCase name}}.element.js";

describe("usa-{{kebabCase name}}", () => {
  it("should be accessible", async () => {
    const {{camelCase name}} = await fixture<USA{{pascalCase name}}Element>(html`
      <usa-{{kebabCase name}}>Hello World</usa-{{kebabCase name}}>
    `);

    return assert.isAccessible({{camelCase name}});
  });
});